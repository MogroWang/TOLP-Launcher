# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] - 2026-09-03

### 新增

- **侧边栏导航**：主界面改为侧边栏 + 三页架构，导航项依次为
  **快速启动**、**版本管理**、**启动器设置**，页面切换带轻量过渡。
- **快速启动页改版**（UI 参考光点之旅菜单设计稿）：
  - 新增游戏 Logo（`tolplogo-v3`）置于启动按钮上方；
  - 原紫色大圆按钮改为**圆角药丸状「启动游戏」按钮**，保留悬停光晕、
    按下即时反馈与加载旋转弧线；
  - 按钮下方依次为**版本选择框**（光点之旅 4.0.002 DEV / 自定义启动）与
    **启动方式选择器**（全屏 / 窗口化）；
  - 游戏就绪状态行保留，路径旁新增**「选择文件夹」快捷按钮**，选择目录后
    自动切换为自定义启动并立即生效；
  - 运行中按钮文案为「回到游戏」，点击可召回游戏窗口。
- **版本管理页**：以占位形式列出内置版本 **光点之旅 4.0.002 DEV**（版本卡片
  含就绪状态与「当前版本 / 设为当前」操作），在线版本分发将于后续版本接入。
- **启动器设置页**：原「启动设置」抽屉的功能（启动方式、游戏目录选择与
  恢复默认）迁移至此，并新增**关于信息**（应用名、版本号、游戏、开发者）。

### 变更

- 内置游戏版本占位标识由 `1.0.0` 更换为 **4.0.002**（前后端同步）；
  旧设置文件中的过期版本号会自动归一化到最新内置版本。
- 标题栏窗口控制在非 Tauri 环境（纯浏览器开发预览）下安全降级，不再整页报错。

### 移除

- 旧主页 Hero 区（大圆球按钮）与「启动设置」右侧抽屉，功能分别由
  快速启动页与启动器设置页承接。

## [0.2.0] - 2026-09-02

### 新增

- **游戏版本管理**：启动设置中新增「游戏版本」选择，可选择内置版本
  （当前为占位符 **1.0.0**）或 **自定义目录** 启动：
  - 选择内置版本时，启动器优先使用 exe 同目录 `games/<版本>/` 托管目录，
    版本尚未提供下载前回落到便携版默认的 `game` 文件夹；
  - 选择自定义目录时，沿用原有的目录指定逻辑；
  - 「游戏目录」设置项仅在自定义模式下显示。
- **游戏运行状态**：游戏窗口正常启动后，主界面显示「正在运行」；
  关闭游戏窗口后自动恢复「开始游戏」状态（通过窗口销毁事件实时通知，
  页面刷新后也会向后端同步真实状态）。运行中点击主球可召回游戏窗口。

### 修复

- 标题栏现在**整个区域均可拖动窗口**（此前仅品牌文字区域可拖动）。

### 变更

- CI 工作流迁移为 `.github/workflows/ci.yml`：推送 `main` 或提交 PR 时自动构建
  Windows x64 便携版作为验证，推送 `v*` 标签时自动创建 GitHub Release 并附上便携版压缩包。

## [0.1.0] - 2026-09-02

### 首个公开版本

- **Tauri 2 + Vue 3 + TypeScript** 桌面启动器，无边框自定义窗口（自绘标题栏，含最小化 / 关闭）。
- 内嵌仅监听 `127.0.0.1` 随机端口的静态文件服务器（tiny_http）提供游戏文件，
  满足 GDevelop 网页版导出必须经 HTTP 访问的要求。
- 点击「开始游戏」按设置以**独立游戏窗口**启动：全屏（无边框）或 1280×720 窗口；
  重复点击复用已有窗口；游戏窗口与启动器相互隔离。
- 启动设置抽屉：全屏 / 窗口化切换、游戏目录选择（默认使用 exe 同目录 `game/` 文件夹）。
- 便携版约定：设置保存于 exe 同目录 `launcher-settings.json`，不写注册表 / AppData。
- GitHub Actions 自动构建 Windows x64 便携版（zip）。

[0.3.0]: ../../releases/tag/v0.3.0
[0.2.0]: ../../releases/tag/v0.2.0
[0.1.0]: ../../releases/tag/v0.1.0
